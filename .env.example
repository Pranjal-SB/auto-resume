# GitHub PAT. Needs `repo` scope only if resume.yaml lists a private repo.
TOKEN=

# Optional: RapidAPI key for the LinkedIn scraper. Leave empty to skip.
LINKEDIN_API_KEY=

# Cache API responses to disk and reuse them while iterating locally.
LOCAL=true
